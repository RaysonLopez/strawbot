services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/models:/app/models
    environment:
      - MODEL_PATH=models/best.pt

  frontend:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - backend
